eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone : http://localhost:1000/eureka
 
server:
  port: 1000
spring:
  application:
    name: Eureka-server
